#!/usr/bin/env bash
cp -f "$PWD/gitconfig" ~/.gitconfig
#cp -f "$PWD/sshconfig" ~/.ssh/config
cp -f "$PWD/bash_profile" ~/.bash_profile
cp -f "$PWD/bashrc" ~/.bashrc
cp -f "$PWD/bash_aliases" ~/.bash_aliases
